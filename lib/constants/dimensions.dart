// set the common device dimensions here for mobile / tablet / desktop

const mobileWidth = 600;
const tabletWidth = 1000;

class AppIconDimensions {
  static const double appIconSystemSize = 20.0;
  static const double appIconMediumSize = 18.0;
}
